# Real-time Data Processing with Apache Kafka

## Overview
A Python producer streams simulated sensor readings, a consumer processes them, and an Avro schema enforces the data format.

## How to Run
1. Start Kafka and Zookeeper with docker compose up -d.
2. Run the producer and consumer scripts in separate terminals.
