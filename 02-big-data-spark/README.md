# Big Data Processing with Apache Spark

## Overview
Analyse a large CSV dataset using PySpark. The example cleans rows, aggregates fares by passenger count, and writes the result as Parquet.

## How to Run
Run the script with a CSV input path and an output path:

```bash
python3 spark_job.py --input taxi_trips.csv --output results.parquet
```
